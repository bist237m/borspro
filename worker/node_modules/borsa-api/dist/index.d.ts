import WatchlistManager = require('./watchlist');
import * as utils from './utils';
import { StockData, IndexData, BorsaAPIOptions } from './types';
/**
 * Borsa API - Turkish Stock Market Data
 * BIST (Borsa İstanbul) verilerini çeker
 */
declare class BorsaAPI {
    private baseURL;
    private timeout;
    private useMockData;
    private useRealData;
    private _provider;
    watchlist: WatchlistManager;
    utils: typeof utils;
    constructor(options?: BorsaAPIOptions);
    /**
     * Mock data generator
     */
    private _generateMockData;
    /**
     * BIST endeks verilerini getirir
     */
    getIndex(symbol?: string): Promise<IndexData>;
    /**
     * Hisse senedi verilerini getirir
     */
    getStock(symbol: string): Promise<StockData>;
    /**
     * Popüler hisseleri getirir
     */
    getPopularStocks(): Promise<StockData[]>;
    /**
     * Tüm BIST endekslerini getirir
     */
    getAllIndexes(): Promise<IndexData[]>;
    /**
     * Türkçe karakterleri normalize eder
     */
    private _normalizeTurkish;
    /**
     * Hisse arama
     */
    searchStock(query: string): Promise<StockData[]>;
    /**
     * İki hisseyi karşılaştır
     */
    compareStocks(symbol1: string, symbol2: string): Promise<{
        stock1: StockData;
        stock2: StockData;
        comparison: {
            priceDiff: number;
            changeDiff: number;
            volumeDiff: number;
        };
    }>;
    /**
     * En çok yükselenleri getir
     */
    getTopGainers(limit?: number): Promise<StockData[]>;
    /**
     * En çok düşenleri getir
     */
    getTopLosers(limit?: number): Promise<StockData[]>;
    /**
     * En yüksek hacimli hisseleri getir
     */
    getTopVolume(limit?: number): Promise<StockData[]>;
    /**
     * Watchlist'teki hisselerin verilerini getir
     */
    getWatchlistData(): Promise<StockData[]>;
    /**
     * Endeks verisini formatlar
     */
    private _formatIndexData;
    /**
     * Hisse verisini formatlar
     */
    private _formatStockData;
    /**
     * Historik fiyat verilerini getirir
     * @param symbol - Hisse sembolü (örn: THYAO)
     * @param options - Zaman aralığı ve interval seçenekleri
     * @returns Historik veri ve meta bilgiler
     */
    getHistoricalData(symbol: string, options?: import('./types').HistoricalOptions): Promise<import('./types').HistoricalData>;
    /**
     * Detaylı hisse bilgilerini getirir (market cap, P/E ratio, vb.)
     */
    getStockDetails(symbol: string): Promise<import('./types').StockDetails>;
    /**
     * Period string'ini başlangıç tarihine çevirir
     */
    private _calculatePeriodStart;
}
export = BorsaAPI;
//# sourceMappingURL=index.d.ts.map